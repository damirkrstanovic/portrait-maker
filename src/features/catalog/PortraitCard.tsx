import type { Portrait, Role } from "../../lib/contracts";
type Props = { portrait: Portrait; role: Role; assetUrl(id: string, role: Role): string; onFocus(id: string): void; onToggleSelection(id: string, selected: boolean): void; selectionLabel?: (portrait: Portrait) => string };
export function PortraitCard({ portrait, role, assetUrl, onFocus, onToggleSelection, selectionLabel }: Props) { return <article className={`portrait-card role-${role}${portrait.selected ? " is-selected" : ""}`} data-testid="portrait-card"><button type="button" className="portrait-card-focus" aria-label={`Preview ${portrait.name}`} onClick={() => onFocus(portrait.id)}><img src={assetUrl(portrait.id, role)} alt="" loading="lazy" /><span className="portrait-card-title">{portrait.name}</span><span className="portrait-card-source">{portrait.sourceName}</span></button><label className="portrait-card-selection"><input type="checkbox" aria-label={selectionLabel?.(portrait) ?? `Select ${portrait.name}`} checked={portrait.selected} onChange={(event) => onToggleSelection(portrait.id, event.target.checked)} /><span>Selected</span></label></article>; }
